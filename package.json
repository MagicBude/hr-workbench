{
  "name": "hr-workbench",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-js.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test"
  }
}
