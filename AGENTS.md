# HR Workbench 协作与编码规则

本文件约束所有在本仓库中工作的开发者和智能体。修改代码前必须先阅读：

1. `docs/ARCHITECTURE.md`：模块边界、数据流和存储结构。
2. `docs/STANDARD.md`：业务数据结构、命名和计算口径。
3. `docs/CODE_COMMENTING_STANDARD.md`：教材型代码、注释与格式规范。
4. `docs/AUDIT_AND_IMPROVEMENT_PLAN.md`：当前风险、优先级和验收清单。

## 项目定位

HR Workbench 是一个零框架、零构建、可离线运行的原生 HTML/CSS/JavaScript 人事工作台。当前数据保存在浏览器 `localStorage`，适合单设备学习、演示和轻量使用，不得把它描述为已经具备企业级账号、权限、审计或正式财税合规能力的系统。

本项目同时承担学习用途：源码既是产品实现，也是中文学习材料。代码是否容易理解，与功能是否正确同样重要。

## 不可破坏的架构边界

- 业务模块不得直接访问 `localStorage`，统一通过 `js/store.js`。
- 可测试的业务口径放在 `js/domain.js`，不得埋进 DOM 事件或页面渲染函数。
- `js/main.js` 只负责初始化、全局事件和视图调度，不承载具体考勤或薪资计算。
- 第三方库必须放在 `vendor/`，不得直接修改压缩后的第三方源码。
- 修改数据结构时，必须同步迁移逻辑、示例数据、导入校验、测试及 `docs/STANDARD.md`。
- 修改业务规则时，必须检查页面、看板、导出和测试是否使用同一口径。

## 教材型编码要求

- 注释使用中文，标识符和 API 名保留英文。
- 每个自有 HTML、CSS、JavaScript、测试和维护脚本都要有文件头。
- 文件头说明职责、输入、输出、协作文件、关键约束和最容易被修改破坏的地方。
- 使用 `#region / #endregion` 按职责分区，标题简短且可以从编辑器大纲直接定位。
- 注释优先解释“为什么、业务口径、边界和副作用”，不得大量复述代码字面行为。
- 复杂函数前说明输入、输出、关键不变量；简单且命名清楚的函数不强制写逐行注释。
- 避免无意义缩写。跨越多行或跨越作用域的变量使用完整语义名称。
- 避免把多个独立操作压在同一行；学习者应能逐行断点调试并看清状态变化。
- HTML 动态内容必须使用安全 DOM API，或按正确上下文转义。
- 安全、兼容性、无障碍、性能和数据迁移取舍必须留下原因说明。
- 修改实现时同步检查相邻注释；过期注释按缺陷处理。

详细例子和检查清单见 `docs/CODE_COMMENTING_STANDARD.md`。

## 验证要求

每次修改后至少运行：

```powershell
npm.cmd run verify
```

涉及 DOM、localStorage、文件导入导出、拖拽或响应式布局时，还必须在本地服务器中做浏览器回归。测试通过不代表注释天然正确；交付前需要再检查注释是否与当前实现一致。

## 提交要求

- 使用 Conventional Commits，例如 `docs: 增加教材型注释规范`、`refactor(domain): 提升领域代码可读性`。
- 每次提交只包含一组相关改动，避免把功能修改、格式化和无关重构混在一起。
- 不覆盖用户已有的无关改动，不修改 `vendor/` 压缩文件。

